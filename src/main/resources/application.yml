server:
  port: 6060

spring:
  application:
    name: data-compare
  
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dianshangpingtai?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password:

# mybatis配置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.entity
  configuration:
    map-underscore-to-camel-case: true 